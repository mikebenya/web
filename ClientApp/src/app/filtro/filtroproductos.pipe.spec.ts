import { FiltroproductosPipe } from './filtroproductos.pipe';

describe('FiltroproductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroproductosPipe();
    expect(pipe).toBeTruthy();
  });
});
